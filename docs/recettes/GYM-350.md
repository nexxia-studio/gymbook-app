# Recette — GYM-350 · Le contrôle de types du dashboard ne contrôlait rien

> Un lot, un fichier. Voir `docs/recettes/README.md`.

🔴 **AUCUN DÉPLOIEMENT.**

🔴 **L'ESSENTIEL DE CE LOT N'EST PAS LE CONTRÔLE DE TYPES.** L'audit a démasqué, dans le
fichier que personne ne contrôlait, un banc de sécurité RLS **inerte à trois niveaux** :
assertion qui ne pouvait pas échouer, salles cibles inexistantes, compteurs périmés. C'est
le seul point où du réel était en jeu.

✅ **Le banc est désormais exécuté et vert** : `32/32, dont 28 qui prouvent quelque chose`.
L'isolation inter-locataires tient, **mesurée** dans les deux sens, en lecture et en écriture.

## Ce que le lot pose

| fichier | changement |
|---|---|
| `apps/dashboard/src/tests/rls-isolation.test.ts` | 🔴 **réécrit** — banc réorienté sur des salles réelles, 32 assertions |
| `apps/dashboard/src/tests/rls-report.ts` | la vacuité est marquée et comptée à part |
| `apps/dashboard/tsconfig.tests.json` | **nouveau** — troisième projet référencé, types Node |
| `apps/dashboard/tsconfig.json` | référence le troisième projet |
| `supabase/functions/get-progression/index.ts` | cast via `unknown` (TS2352) — **aucun changement de comportement** |
| `docs/ops/preuves-controle-de-types.md` | **nouveau** — la convention |

---

## § 1 — 🔴 Deux assertions RLS qui ne pouvaient pas échouer

### Le défaut

```ts
.update({ name: 'HACKED' }).eq('gym_id', MOVE95_ID)
.select('id', { count: 'exact', head: true })     // ← le 2e argument est IGNORÉ
…
(count ?? 0) === 0                                 // ← donc TOUJOURS vrai
```

Après `.update()` / `.delete()`, on n'est plus sur le `PostgrestQueryBuilder` — dont
`select` accepte `(columns, options)` — mais sur le **`PostgrestTransformBuilder`**, dont
`select` **n'accepte qu'un argument**.

Établi sur le code installé (`@supabase/postgrest-js`, supabase-js 2.105.4) :

| | |
|---|---|
| `PostgrestQueryBuilder.select(columns?, options?)` | `index.d.mts:2890` — 2 arguments |
| `PostgrestTransformBuilder.select(columns?)` | `index.d.mts:1093` — **1 argument** |
| implémentation d'exécution | `index.mjs:556` — `select(columns)`, le 2e argument **n'est jamais lu** |

Aucun en-tête `count` n'est donc posé : `count` vaut `null`, et `(count ?? 0) === 0` est vrai
**quoi qu'il arrive**. Les deux tests concernés —

- `Studio Test CANNOT UPDATE Move95 coaches`
- `Studio Test CANNOT DELETE Move95 activities`

— **passaient au vert avec les RLS grandes ouvertes.** Ce sont exactement les deux
protections en écriture inter-locataires.

### Le correctif

On compte les lignes **renvoyées**. `.select('id')` pose déjà `Prefer: return=representation` :
la liste est vide quand RLS bloque.

```ts
const { data: updRows, error: updErr } = await studio
  .from('coaches').update({ name: 'HACKED' }).eq('gym_id', MOVE95_ID)
  .select('id')
…
!!updErr || (updRows?.length ?? 0) === 0
```

Le `!!updErr` couvre le cas où la politique **lève** au lieu d'affecter 0 ligne. Un UPDATE ou
DELETE bloqué par RLS ne lève pas en général — il affecte 0 ligne — d'où le recours initial
au comptage, et d'où l'erreur.

### ⚠️ Correction de comptage

J'avais annoncé « 13 assertions, dont 11 saines ». **C'était faux** : 13 est le nombre de
**sites d'appel** `test(`, mais `testTableIsolation` en contient 2 et **était appelée 6
fois** — soit **23 assertions à l'exécution**. Le banc réécrit en compte **32**.

### § 1 bis — 🔴 Le banc pointait aussi sur des salles INEXISTANTES

Corriger l'assertion ne suffisait pas : le banc était **doublement inerte**.

Mesuré en staging :

| identifiant codé en dur | réalité |
|---|---|
| `a0000000-0000-0000-0000-000000000001` | **salle de PRODUCTION** (Dopamine Performance Club) — absente de staging |
| `b0000000-0000-0000-0000-000000000002` | n'existe **nulle part** |

Toute lecture « inter-locataires » interrogeait une salle absente : **0 ligne, toujours, RLS
ou pas**. Et six **compteurs en dur** (« 8 activities », « 5 coaches », « 4 plans ») ne
correspondaient ni à staging, ni même à la prod d'aujourd'hui (6 / 6 / 7).

Pire, l'assertion `Studio Test admin sees 0 {label}` n'était **pas un test d'isolation** mais
une hypothèse de jeu de données — « l'autre salle est vide » — fausse dès qu'elle contient
quoi que ce soit. Supprimée.

### Le banc réécrit

Deux **gérants** (`gym_admin`), pas deux membres — le banc les nommait « admin » mais
utilisait des comptes `role = member`, qui n'ont pas la même surface RLS.

| rôle | compte | salle |
|---|---|---|
| attaquant / victime | `admin.clone@staging.test` | Dopamine (Staging Clone) |
| attaquant / victime | `nexxia.studio+salletest1@gmail.com` | Studio Yoga Test 1 |

⚠️ **`admin.dopamine@staging.test` est gérant de Studio Test Staging malgré son nom** — piège
réel, écarté.

Variables d'environnement : `STAGING_ADMIN_CLONE_EMAIL` / `_PASSWORD`,
`STAGING_ADMIN_YOGA_EMAIL` / `_PASSWORD`, `STAGING_ANON_KEY`.

**Les trois leçons inscrites dans le code :**

1. **Aucun compteur en dur** — on vérifie que *toutes* les lignes vues portent *son* `gym_id`.
   Cette formulation ne périme pas.
2. **La vacuité est marquée**, plus jamais tue : `<< À VIDE — ne prouve rien`, et comptée à
   part dans le total.
3. **Le bon motif d'échec est exigé** : l'INSERT doit être refusé en **42501** (refus RLS).
   Une violation `NOT NULL` ou de clé étrangère passerait sinon pour une preuve d'isolation.

⚠️ **Les tentatives destructrices visent Studio Yoga Test 1** (1 activité, 1 coach, 0
réservation). Le sens Yoga → Dopamine se limite à un INSERT : un UPDATE ou un DELETE sur
Dopamine (Staging Clone) détruirait, en cas de faille, des données portant **293 créneaux et
28 réservations**. Asymétrie voulue et commentée dans le code.

### ✅ LE BANC A ÉTÉ EXÉCUTÉ — et il passe

```
32/32 tests passed | ALL CLEAR
dont 28 qui prouvent quelque chose | 4 à vide (cible sans données)
```

**L'isolation tient, mesurée dans les deux sens** : lecture et écriture, sur les six tables.
Les 4 assertions à vide visent `bookings` et `member_subscriptions` de Yoga, réellement vides
— elles le disent au lieu de se faire passer pour des preuves.

Staging vérifié intact après coup : compteurs inchangés, **zéro ligne `HACKED`**, aucun insert
orphelin.

### Le banc peut échouer — prouvé

Attaquant pointé sur sa propre salle : **exit 1, 6 échecs critiques**. C'est la première fois
qu'on peut l'affirmer de ce banc.

## § 2 — `src/tests` n'était contrôlé par rien

`tsconfig.app.json` porte `"exclude": ["src/tests"]`, et aucun autre projet ne les reprenait.
**Deux fichiers hors de tout contrôle** — ceux du § 1.

Le lot ajoute `tsconfig.tests.json`, troisième projet référencé, avec les types **Node**
(le banc tourne sous `tsx`, pas dans le navigateur). L'`exclude` de `tsconfig.app.json`
**reste** : les fichiers appartiennent désormais au troisième projet, l'exclusion évite la
double inclusion.

### Le piège est refermé — prouvé

Bug d'origine réintroduit délibérément, puis `npx tsc --build` :

```
src/tests/rls-isolation.test.ts(206,19): error TS2554: Expected 0-1 arguments, but got 2.
exit=2
```

**C'est exactement l'erreur qui aurait arrêté le § 1**, des semaines plus tôt.

### Cinq alias resserrés, sans changement de comportement

Les gardes `if (!X) throw` existaient déjà et suffisent à l'exécution, mais ne resserrent pas
le type : `signIn` et `main` sont des **déclarations** de fonction, donc hoistées, et le
resserrement obtenu au niveau du module ne les traverse pas. Cinq `const` figent les valeurs
après les gardes. **Messages d'erreur et comportement rigoureusement identiques.**

---

## § 3 — `get-progression` ne compilait pas

2 × `TS2352` (lignes 171 et 191), issues de **GYM-57** — pas des lots de la semaine. La
fonction est **en production**, consommée par l'onglet studio (`apps/mobile/hooks/useProgression.ts`).

Jamais vue parce que `deno check` n'a toujours été lancé que sur les fonctions touchées par
chaque lot.

**Diagnostic : bruit de typage, sans impact d'exécution.** `bookings → time_slots` est une
relation vers-un : PostgREST rend un objet ; ce sont les types générés qui l'annoncent en
tableau. Le code se protège déjà (`Array.isArray(ts) ? ts : [ts]`).

**Correctif : `as unknown as`** — le remède que TypeScript indique lui-même dans le message
TS2352. Un cast est **entièrement effacé à la compilation**. Diff hors commentaires, les deux
seuls changements :

```diff
-        const ts = (row as { time_slots: … }).time_slots
+        const ts = (row as unknown as { time_slots: … }).time_slots
```

⚠️ **La lecture défensive est laissée telle quelle.** `arr[0]?.activities?.name` rendrait
`undefined` si `activities` était un tableau. Traiter ce cas serait un **changement de
comportement** sur une fonction en production — hors cadrage de ce lot. Si `cours_favori` ou
`coach_favori` remontent vides en production, c'est la première piste.

---

## § 4 — La convention

`docs/ops/preuves-controle-de-types.md` : `tsc -b` côté dashboard (jamais `--noEmit`),
`deno check` sur **les 35** fonctions, comment lire les bancs Deno, quand déclarer un banc SQL
non joué, et le motif commun.

---

## § 5 — Les recettes historiques ne sont pas retouchées

GYM-330, GYM-336, GYM-337, GYM-338, GYM-348 portent « `tsc` dashboard **exit 0** ». **Cette
mention est creuse** : la commande ne contrôlait aucun fichier.

Décision : **ne pas réécrire ces recettes.** Elles disent ce qui a été fait à l'époque ; les
corriger effacerait la trace de l'erreur. Cette note en tient lieu.

**Ce que valait réellement cette preuve, et ce qu'elle vaut aujourd'hui :**

| | |
|---|---|
| `npx tsc --noEmit --listFiles` sur le dashboard | **0 ligne** |
| `npx tsc --build` après ce lot | **154 fichiers**, 0 erreur |

✅ **Aucun lot dashboard de la semaine n'est passé à travers.** Les six fichiers touchés
(`MollieConnectCard.tsx`, `legalEditor.ts`, `PaymentCancel.tsx`, `PaymentSuccess.tsx`,
`en.json`, `fr.json` — GYM-333a, GYM-105) sont tous dans les 151 du projet applicatif, et
`tsc -b` rend 0 erreur. **La preuve était vide, le code était sain.**

✅ **La 1.2.1 soumise à Apple n'est pas concernée.** `apps/mobile/tsconfig.json` a un `include`
réel : 164 fichiers projet contrôlés, **0 fichier hors contrôle**. Le mobile a bien été vérifié.

⚠️ Le « 1952 fichiers » annoncé le 16/09 était le total `--listFiles`, **`node_modules`
compris** — douze fois la couverture réelle. Chiffre honnête : **164**.

---

## Preuves

- 🔴 **Banc RLS EXÉCUTÉ contre staging** : `32/32 tests passed | ALL CLEAR`, `exit 0` — dont
  **28 qui prouvent quelque chose** et 4 marquées à vide (tables réellement vides)
- 🔴 **Le banc peut échouer, prouvé** : attaquant pointé sur sa propre salle → **exit 1,
  6 échecs critiques**
- 🔴 **Staging intact après les tentatives destructrices** : compteurs inchangés sur les trois
  salles, **zéro ligne `HACKED`**, aucun insert orphelin
- **Deux assertions vides**, établies sur le code installé : typages `index.d.mts:1093`
  (1 argument) vs `:2890` (2 arguments), et implémentation `index.mjs:556` qui **ne lit
  jamais** le 2e argument — `count` vaut `null`, l'assertion est vraie quoi qu'il arrive
- **Salles cibles mesurées, pas devinées** : `a0000000-…-0001` est la salle de **production**
  (Dopamine Performance Club) ; `b0000000-…-0002` n'existe nulle part
- **Comptes vérifiés** : les deux comptes d'origine étaient `role = member`, pas admin ;
  `admin.dopamine@staging.test` est gérant de **Studio Test Staging** malgré son nom
- **Piège de types refermé, prouvé par falsification** : bug d'origine réintroduit →
  `tsc --build` rend `TS2554`, exit 2
- **Couverture dashboard** : 151 + 1 + 2 = **154 fichiers**, **0 fichier hors contrôle**,
  `tsc --build` **exit 0**
- **`tsc` mobile** exit 0 — 164 fichiers projet, 0 hors contrôle
- **`deno check` sur les 35 fonctions** → **0 en échec** (34/35 avant ce lot)
- **`deno check` est une vraie preuve**, vérifié par falsification : erreur de type injectée
  dans `_shared/commission.ts` → attrapée depuis `create-subscription` (suit le graphe)
- **Bancs Deno vérifiés** : `renewal-date_test.ts` **18 cas**, `recovery-redirect_test.ts`
  **12 cas**. `early-performance_test.ts` affiche `0 passed` — **trompeur mais pas creux** :
  corps exécuté en « pre-test output », **exit 1** confirmé en cassant la fonction testée
- **Bancs SQL honnêtes** : `gym338_adhesion_a_la_source.sql` se déclare lui-même NON EXÉCUTÉ
- **`get-progression`** : `deno check` **exit 0**, diff hors commentaires limité à
  `unknown as` sur deux lignes — **aucun changement de comportement**
- **Comptage corrigé** : l'ancien banc avait **23** assertions à l'exécution (et non 13, qui
  était le nombre de sites d'appel) ; le nouveau en a **32**
- **Recettes historiques non retouchées** · aucun déploiement
